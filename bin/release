#!/usr/bin/env bash
####
#
# Provides metadata back to the runtime
#
# Usage:
#
# ```shell
# bin/release BUILD_DIR
# ```
BUILD_DIR=$1

echo "-----> bin/release"

cat << EOF
---
addons: []
config_vars:
  PORT: 6667
default_process_types:
  znc: bin/run
EOF
