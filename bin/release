#!/usr/bin/env bash
####
#
# Provides metadata back to the runtime
#
# Usage:
#
# ```shell
# bin/release BUILD_DIR
# ```
BUILD_DIR=$1

echo "-----> bin/release"

cat << EOF
---
addons: []
default_process_types:
  znc: bin/run
EOF
